export class Task {
  constructor (title, dueDate, priority, finishedTask) {
    this.title = title;
    this.dueDate = dueDate;
    this.priority = priority;
    this.finishedTask = finishedTask;
  }
}